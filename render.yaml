services:
  - type: web
    name: meritbadge-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: AI_KEY
        sync: false
